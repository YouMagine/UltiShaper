// Generated by CoffeeScript 1.6.2
define(function(require) {
  var $, CodeEditor, CodeEditorSettings, _;

  $ = require('jquery');
  _ = require('underscore');
  CodeEditor = require('editors/codeEditor/codeEditor');
  CodeEditorSettings = require('editors/codeEditor/codeEditorSettings');
  return describe("codeEditor", function() {
    var editorSettings, project;

    project = null;
    editorSettings = null;
    beforeEach(function() {
      var settings;

      project = new Project({
        name: "test_project"
      });
      project.create_part({
        name: "testPart"
      });
      project.create_part({
        name: "otherPart"
      });
      settings = new CodeEditorSettings();
      return settings.set("startLine", 7);
    });
    it('accepts previously defined settings', function() {
      var codeEditor;

      codeEditor = new CodeEditor({
        project: project,
        settings: settings
      });
      return expect(codeEditor.settings.get("startLine")).toBe(7);
    });
    return it('creates as many tabs as files are passed to it', function() {});
  });
});
