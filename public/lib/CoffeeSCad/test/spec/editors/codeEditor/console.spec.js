// Generated by CoffeeScript 1.6.2
define(function(require) {
  var $, ConsoleView, vent, _;

  $ = require('jquery');
  _ = require('underscore');
  vent = require('core/messaging/appVent');
  ConsoleView = require('editors/codeEditor/consoleView');
  return describe("console", function() {
    var consoleView;

    consoleView = null;
    beforeEach(function() {
      return consoleView = new ConsoleView();
    });
    it('changes style depending on error', function() {
      var consoleClass;

      vent.trigger("error", null);
      consoleClass = consoleView.$el.attr('class');
      return expect(consoleClass).toBe("alert alert-error");
    });
    return it('clears when there are no errors', function() {
      var consoleClass;

      vent.trigger("noError");
      consoleClass = consoleView.$el.attr('class');
      return expect(consoleClass).toBe("well");
    });
  });
});
