// Generated by CoffeeScript 1.6.2
define(function(require) {
  var Library, Project;

  Library = require("core/projects/library");
  Project = require("core/projects/project");
  return describe("library", function() {
    var library;

    library = null;
    beforeEach(function() {
      return library = new Library();
    });
    it('can list projects from different types of storage', function() {
      var allProjects;

      allProjects = library.getProjectByStore("all");
      return expect(allProjects).toEqual([]);
    });
    return it('can save a project to browser (localstorage)', function() {
      var project;

      project = new Project({
        name: "testProject"
      });
      return project.createFile({
        name: "testFile",
        content: "someContent"
      });
    });
  });
});
